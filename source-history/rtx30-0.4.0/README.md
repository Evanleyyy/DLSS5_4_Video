# 30 系独立版的历史源码差异

当前开发统一使用主工程根目录的 `source`、`packaging`、`tests` 和 `tools`。

本目录仅保存原 0.4.0 独立工程中尚未提交的安装配置、说明和验证脚本，共 7 个原始文件，以及 `working-tree.patch`。`manifest.json` 记录逐文件 SHA-256；复制后已核对一致。历史脚本保留原内容，不能从此归档目录直接当作当前工程运行。

独立版的基础提交为 `c1f1b787a35afa4749d69b49b22b816466fe4ee2`，已包含在主工程 Git 历史中。需要研究原版时，可先在另外的临时目录导出该提交，再应用补丁并复制这里的新增文件。无需保留整套重复的运行库、模型和构建目录。

0.4.0 安装包及其分卷统一保存到主工程 `output/rtx30-0.4.0/`。当前开发与打包入口仍位于主工程根目录。
