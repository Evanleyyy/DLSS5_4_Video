# DLSS5 V2 代码审查与本地部署

检查日期：2026-09-15。

本文保留首次部署时的审查记录；后续界面与多通道导出功能、当前启动步骤以根目录 `README.md` 及 `docs/` 为准。文中 `original-source/`、`logs/` 和完整运行资源保存在本机，未纳入 Git。

## 结论

项目存在会影响稳定性、内存占用、引导质量和导出可靠性的问题。本地已修复下表中的 Python 问题，独立运行环境与图形界面均已验证。

**安全审计仍有限制：发布包的 `nvngx_dlssnr.dll` 数字签名校验为 `HashMismatch`；`dlssnr_host.dll` 未签名，也没有配套原生源码。无法据此保证整套程序安全。** 签名异常本身不能证明存在恶意行为；本次没有修改这两个 DLL，也没有修改驱动或系统安全设置。

## 启动方法

双击 [启动DLSS5.bat](./启动DLSS5.bat)。程序是本地图形界面，没有浏览器访问地址。

1. 图片：点击“导入图片”，处理完成后点击“导出图片”。修改参数后会重新生成。
2. 视频：点击“导入视频”，保持引导模式为“关闭”即可直接运行 DLSS。
3. 若需要引导：先生成深度和光流，选择“深度+光流”，再运行 DLSS。
4. 导出区域选择 `dlss`，点击“导出视频”。导出会按当前参数重新处理，防止使用旧参数的结果。
5. 结果保存在原素材旁的 `<名称>_dlss` 文件夹及 `<名称>_dlss.mp4`。深度和光流也会在素材旁生成缓存。

启动错误查看 `logs/gui.log`；原生运行记录位于 `source/dlss5standaloneV2/dlssnr_run.log`。

## 来源与完整性

- 项目：[purkatyy/DLSS5-](https://github.com/purkatyy/DLSS5-)。
- 发布：[DLSS5 standalone V2 / dlss5v2](https://github.com/purkatyy/DLSS5-/releases/tag/dlss5v2)，2026-09-10。
- 发布附件 `DLSS5standaloneV2.rar` 为 1,378,114,867 字节。
- 本机已有完整附件，实际计算 SHA256 与 GitHub 公布值完全相同：

```text
5b930947a0d0ab66b20109a99ee1e4182b3b03d7b697b27388da9386c5691358
```

主仓库只有说明和许可证，应用 Python 源码、模型与 DLL 在发布附件中。`original-source/` 保存此次修改前的核心 Python 文件、依赖清单及启动器。完整原始附件仍在用户原下载目录中。

二进制 SHA256：

| 文件 | SHA256 | 签名结果 |
| --- | --- | --- |
| `dlssnr_host.dll` | `9ec879388e573bebbf7886dccbdc885641d945420fdf4bb80bac6c657094160c` | 未签名 |
| `nvngx_dlssnr.dll` | `ceb6432f6fbdf44d886014bcd47241932bf8b67439feef9bbdd0961436662650` | `HashMismatch` |

发布附件的哈希一致只能确认与作者发布内容相同，不能证明 DLL 与 NVIDIA 官方原文件相同。详细签名结果保存在 `logs/dll-signatures.json`。

## 主要代码问题与处理

以下行号指向 `original-source/` 中的原始版本。

| 严重程度 | 原始位置 | 问题与触发条件 | 本地处理 |
| --- | --- | --- | --- |
| 高 | `gui.py:625`、`635` | 全片图像、光流和深度同时装入列表，再保留所有输出；1080p、30 帧/秒、一分钟的输入数组理论上约占 56 GiB，尚未计入输出 | 改为逐帧读取、推理、写盘 |
| 高 | `dlss_engine.py:98`、`168` | 未校验形状、类型和连续性就把数组地址交给原生 DLL；错误尺寸或缓存可能导致原生越界读取 | 校验尺寸、类型、有限数值，转换为连续内存；关闭后拒绝调用 |
| 高 | `gui.py:566`、`dlss_engine.py:120` | 后台任务与实时预览共享进程全局 DLL 会话，可能重复初始化或相互释放；后台线程还直接修改 Tk 控件 | 独占会话锁；任务开始前关闭预览；工作期间禁用冲突操作；通过队列在主线程更新界面 |
| 高 | `pipeline.py:389`、`391`、`gui.py:649` | 编码失败被自身的异常处理吞掉，仍可能返回输出路径；DLSS 错误也可能随后被“完成”状态覆盖 | 传播错误，使用临时文件，编码成功后才替换输出；保留既有成功文件 |
| 中 | `pipeline.py:345` 附近 | 导出时跳过缺失帧，造成缩短视频和音画错位；旧缓存也不核对原片 | 缺帧或损坏立即报错；记录原片路径、大小、修改时间及生成参数；未完成缓存拒绝导出 |
| 中 | `pipeline.py:205` | 调用端先将 BGR 转 RGB，`DepthAnythingV2.image2tensor()` 又转换一次，导致输入红蓝通道颠倒 | 将 BGR 直接交给模型；与模型源代码的预处理方式一致 |
| 中 | `pipeline.py:283` | 将“上一帧到当前帧”的光流简单取负，无法得到当前像素坐标上的正确反向光流，运动边界尤其易错 | 直接计算“当前帧到上一帧”的光流 |
| 中 | `pipeline.py:115` | 按图像最大值判断深度位数，低值的 16 位图或最大值为 1 的 8 位图会被错误归一化 | 按 `uint8` / `uint16` 类型归一化 |
| 中 | `gui.py` 多处 | Windows 中文图片路径可能读取失败；图片写入失败也可能显示成功 | 通过字节读取及 `imdecode` / `imencode` 处理路径，写入失败抛出异常 |
| 中 | `run_gui.bat`、`requirements.txt` | 启动器只检查解释器名称，未验证依赖；版本无上限且没有锁定 CUDA 配套版本 | 使用项目独立 Python，锁定已实测版本，补充 Pillow 直接依赖 |

另外增加了命令行 `--dlss` 和 `--guidance`，模型权重加载显式使用 `weights_only=True`，图片参数刷新也会实际重新推理。

## 本机运行环境

| 项目 | 已验证配置 |
| --- | --- |
| 显卡 | NVIDIA GeForce RTX 4090 Laptop GPU，16 GB 显存 |
| 驱动 | 596.36 |
| Python | 3.12.10，项目内 `runtime/python/` |
| 虚拟环境 | 项目内 `.venv/`，不继承系统包 |
| PyTorch / CUDA | `2.8.0+cu128` / CUDA 12.8 |
| Torchvision | `0.23.0+cu128` |
| OpenCV / NumPy | `4.13.0.92` / `2.4.3` |
| Pillow | `12.1.1` |
| FFmpeg | 项目内 `runtime/ffmpeg.exe`，已验证 H.264 与 AAC 导出 |

Python、CUDA PyTorch 及依赖从本机已有环境复制到项目中，缺少的 OpenCV 和诊断依赖安装到独立虚拟环境。最终运行不依赖原来的 Woosh 目录。完整依赖清单保存在 `requirements-local.lock.txt`。

该虚拟环境绑定当前绝对路径；移动整个项目目录后需要重新建立 `.venv`。本次没有修改其他项目环境、系统 Python、显卡驱动，没有创建提交或推送。

## 实际验证结果

- 7 项回归测试通过：原生接口非法输入、并发会话、重复关闭、帧处理失败、中文路径/深度归一化、缺帧导出、编码失败保护。
- `uv pip check`：16 个包依赖兼容。
- 实际 GPU 流程：320×240、24 帧/秒、12 帧测试片；无引导 DLSS 约 1.02 秒完成。
- 3 帧深度约 6.56 秒，光流约 0.45 秒，深度+光流引导的 DLSS 约 0.72 秒；均生成有效输出。
- 导出帧数验证通过，FFmpeg 确认输出含音轨；输出像素与原图确实不同。
- 深度、光流缓存重复读取通过。
- 隐藏窗口界面测试通过：启动、317×239 奇数尺寸图片、参数刷新、视频处理、导出。

详细数据：`logs/smoke-result.json`。界面验证：`logs/gui-smoke.log`。

运行自检可双击 `运行自检.bat`。测试只生成并处理项目内的测试素材，不处理用户的视频。

### 沙箱诊断结果

在本次工具沙箱中，原始代码和本地修复版调用原生 `shutdown` 均会卡住；正常本机进程约 1 秒完成整个单帧生命周期。增加等待或跳过帧处理不能消除沙箱内问题。原版与修复版差分测试因此将本次卡住定位到沙箱运行条件，而非此次 Python 修改。

最终 GPU 与界面验证均在正常本机进程中完成。日常直接双击启动器；不需要把程序放在本次工具沙箱中运行。最小复现代码保留在 `tests/native_lifecycle.py`，证据在 `logs/native-lifecycle-original.txt`。

## 未覆盖和剩余限制

- 本次审查覆盖发布包中的 Python 源码、依赖、文件签名与实际调用流程，没有完成 DLL 反汇编审计或恶意代码全量分析；不能承诺二进制安全。
- 仅在本机 RTX 4090 Laptop 上实测，未验证 30/50 系显卡、其他驱动、HDR、可变帧率、超宽幅、4K 长片或长时间稳定性。
- 光流仍按原代码短边 720 的规则缩放，极端宽高比可能占用较多显存；深度/光流/PNG 缓存也会占用大量磁盘空间。
- 同一图片文件夹里如果有同名不同扩展名的图片，原来的批处理命名仍可能覆盖同名输出；建议将这类输入分到不同文件夹。
- 修改参数后的 DLSS 导出会重新生成所有帧；有更长等待，但能避免导出旧参数结果。

模型预处理依据：[Depth Anything V2 源码](https://github.com/DepthAnything/Depth-Anything-V2/blob/main/depth_anything_v2/dpt.py)。光流接口依据：[Torchvision RAFT 文档](https://docs.pytorch.org/vision/stable/models/generated/torchvision.models.optical_flow.raft_large.html)。
